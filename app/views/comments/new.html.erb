<%= turbo_frame_tag nested_dom_id(@comment.commentable, :new_comment_frame) do %>
  <div class="max-w-6xl mx-auto mt-6 bg-white border border-neutral-200 rounded-xl p-6">
    <%= render partial: 'form', locals: { comment: @comment } %>
  </div>
<% end %>