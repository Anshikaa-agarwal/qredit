<%= turbo_stream.update 'flash', partial: 'shared/flash' %>

<%= turbo_stream.prepend nested_dom_id(@comment.commentable, :comments_frame), partial: 'comments/comment', locals: { comment: @comment } %>
