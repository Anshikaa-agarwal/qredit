<%= turbo_stream.update 'flash', partial: 'shared/flash' %>
<%= turbo_stream.remove dom_id(@comment) %>
<%= turbo_stream.replace dom_id(@comment.commentable, :comments_count), @comment.commentable.comments.count %>