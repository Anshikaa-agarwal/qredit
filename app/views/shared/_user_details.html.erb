<div class="flex items-center gap-4">
    <%= image_tag(user.avatar, class: "w-12 h-12 rounded-full border border-gray-300 object-cover") %>

    <div>
        <p class="text-lg font-semibold text-gray-800"><%= user.name %></p>
        <p class="text-sm text-gray-600"><%= user.followers.count %> followers</p>
    </div>
</div>
  